apiVersion: v1
kind: Service
metadata:
  name: {{ include "anvilkit-agent-api.fullname" . }}
  labels:
    {{- include "anvilkit-agent-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "anvilkit-agent-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
